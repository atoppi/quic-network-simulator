diff --git a/interop/server/main.go b/interop/server/main.go
index 867044b4..291c9612 100644
--- a/interop/server/main.go
+++ b/interop/server/main.go
@@ -19,13 +19,14 @@ import (
 var tlsConf *tls.Config
 
 func main() {
-	logFile, err := os.Create("/logs/log.txt")
-	if err != nil {
-		fmt.Printf("Could not create log file: %s\n", err.Error())
-		os.Exit(1)
-	}
-	defer logFile.Close()
-	log.SetOutput(logFile)
+	//logFile, err := os.Create("/logs/log.txt")
+	//if err != nil {
+	//	fmt.Printf("Could not create log file: %s\n", err.Error())
+	//	os.Exit(1)
+	//}
+	//defer logFile.Close()
+	//log.SetOutput(logFile)
+	log.SetOutput(os.Stderr)
 
 	keyLog, err := utils.GetSSLKeyLog()
 	if err != nil {
@@ -46,7 +47,10 @@ func main() {
 	// a quic.Config that doesn't do a Retry
 	quicConf := &quic.Config{
 		RequireAddressValidation: func(net.Addr) bool { return testcase == "retry" },
-		Tracer:                   qlog.NewTracer(getLogWriter),
+		Tracer:                   nil,
+	}
+	if getLogWriter != nil {
+		quicConf.Tracer = qlog.NewTracer(getLogWriter)
 	}
 	cert, err := tls.LoadX509KeyPair("/certs/cert.pem", "/certs/priv.key")
 	if err != nil {
